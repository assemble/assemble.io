{{!#is area 'boilerplates'}}
{{!-- MASTHEAD
=================================== --}}
<header class="masthead subhead">
  <div class="container">
    <div class="row">
      <div class="col col-lg-12">
        <h1>
          {{#if title}}
            <span class="text-muted">BOILERPLATES&nbsp;/</span>&nbsp;
            {{ title }}
          {{else}}
            BOILERPLATES
          {{/if}}
        </h1>
      </div>
    </div>
  </div>
</header>
{{!/is}}
