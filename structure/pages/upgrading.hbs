---
layout: content
title: Migrations
slug: helpers
---
<!-- See `data/generated-pages/upgrading.yml` -->
{{#each upgrading.published}}
  <!-- {{title}} -->
  <div class="docs-section">
    <div class="page-header">
      <h1 id="{{@key}}">{{title}}</h1>
    </div>

    {{#each (expand files)}}
      <!-- {{basename .}} -->
      {{success '>> Inlining:' (basename .)}} {{! log a success message! }}

      {{#markdown}}
        {{inline .}}
      {{/markdown}}

      <br>
    {{/each}}

  </div>
{{/each}}