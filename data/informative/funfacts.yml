helpers:
  top:
    - "Exactly <%= _.fileCount('content/helpers/**/*.md') %> files were used create this page the last time it was built."
    - "This page was converted from markdown."
    - "When this page is built by Assemble, all <%= _.fileCount('content/helpers/**/*.md') %> files are concatenated, converted to HTML, wrapped with a layout, templates are compiled and the final pages rendered!"
    - "The file count in the first fact is automatically generated by the `_.fileCount` Lo-Dash mixin, found in the data/utils directory of the assemble.io project."
